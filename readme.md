# Welcome to SIL - SearchIntLaw. #
All uses are for self-learning, sites will be taken out of the project upon request.
1. to update json & index, run app.py
2. to create users allowed to login, run src/mng_users.py and define name & password
3. browse the site locally on http://127.0.0.1:5000 and login with user info
4. easily deployabe with render for public access. note-> preferable to run app.py locally and then upload to render, faster articles.json & indexdir creation